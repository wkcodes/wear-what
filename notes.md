Make a white rounded border for all components('card' style) and put them on a solid color background. Maybe make this styling a component and wrap it around all components. 

Make current 'Wrapper' components into 'AppWrapper'